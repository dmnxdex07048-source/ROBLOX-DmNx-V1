local UIS = game:GetService("UserInputService"); local plt = game.Players.LocalPlayer; local sayMsg = "⚜️𝐃ᴍ𝐍x MUSIC LOADED⚜️"; local chatRem = game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest; chatRem:FireServer(sayMsg, "All"); local chr = plt.Character or plt.CharacterAdded:Wait(); local rem = game:GetService("ReplicatedStorage").RemoteEvents.PlayerNameDone; rem:FireServer("🎶𝐃ᴍ𝐍x MUSIC HUB🎶", "WELCOME DEAR DmNx_Ji"); local SG = Instance.new("ScreenGui"); SG.Name = "DmNxMusic"; SG.Parent = game.CoreGui; SG.ResetOnSpawn = false; local MF = Instance.new("Frame"); MF.Name = "MainFrame"; MF.Parent = SG; MF.BackgroundColor3 = Color3.fromRGB(15, 15, 15); MF.Position = UDim2.new(0.5, -250, 0.5, -175); MF.Size = UDim2.new(0, 500, 0, 350); MF.Active = true; MF.Draggable = true; local UC = Instance.new("UICorner"); UC.CornerRadius = UDim.new(0, 10); UC.Parent = MF; local SB = Instance.new("Frame"); SB.Name = "Sidebar"; SB.Parent = MF; SB.BackgroundColor3 = Color3.fromRGB(20, 20, 20); SB.Size = UDim2.new(0, 120, 1, 0); local Lbl = Instance.new("TextLabel"); Lbl.Text = "DmNx MUSIC"; Lbl.Size = UDim2.new(1, 0, 0, 40); Lbl.TextColor3 = Color3.fromRGB(255, 255, 255); Lbl.BackgroundTransparency = 1; Lbl.Font = Enum.Font.GothamBold; Lbl.Parent = SB; local Cls = Instance.new("TextButton"); Cls.Size = UDim2.new(0, 30, 0, 30); Cls.Position = UDim2.new(1, -40, 0, 10); Cls.BackgroundColor3 = Color3.fromRGB(255, 50, 50); Cls.Text = "X"; Cls.TextColor3 = Color3.fromRGB(255, 255, 255); Cls.Parent = MF; Cls.MouseButton1Click:Connect(function() SG:Destroy() end); print("𝐃ᴍ𝐍x Music V1 Loaded!")
